# Unified Claude design release

Use Claude's Ledger & Light reference 934e5cb14bfaf8dedd1b6adb0a7bcbd87d10267f for Android and iOS. Retain newer spending-first Home, category shares, separately grouped Bills, offline merchant logos, background history coordinator, durable persistence fix and guided iOS setup.

Both native builds must use the same reviewed source commit. No production OTA, phone installation, ledger deletion or public store submission is authorized by this source integration.

Validation is not complete until source, browser and native checks have actually passed. A successful build is not proof of physical-phone frame pacing or SMS throughput.
