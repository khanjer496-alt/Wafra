<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- Home-screen widgets. They read only the summary snapshot the app writes
         (see android/README.md), never the ledger or messages. Widget receivers
         must be exported so the launcher's AppWidgetHost can bind them. -->
    <receiver
      android:name="expo.modules.wafrawidgets.TodayWidgetProvider"
      android:exported="true"
      android:label="@string/wafra_widget_today_name">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        <action android:name="android.intent.action.TIME_SET" />
        <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        <action android:name="android.intent.action.LOCALE_CHANGED" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/wafra_widget_today_info" />
    </receiver>
    <receiver
      android:name="expo.modules.wafrawidgets.UpcomingWidgetProvider"
      android:exported="true"
      android:label="@string/wafra_widget_upcoming_name">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        <action android:name="android.intent.action.TIME_SET" />
        <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        <action android:name="android.intent.action.LOCALE_CHANGED" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/wafra_widget_upcoming_info" />
    </receiver>
  </application>
</manifest>
