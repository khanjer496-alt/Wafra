name: Dispatch exact-source CI with live ref once

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/dispatch-exact-ci-keep-ref-once.yml'

permissions:
  actions: write
  contents: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Run CI on the exact final iOS source commit
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run ci.yml --repo "$GITHUB_REPOSITORY" --ref ci/final-ios-5fbe5083
      - name: Remove this temporary workflow from main
        run: |
          rm -f .github/workflows/dispatch-exact-ci-keep-ref-once.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .github/workflows
          git commit -m 'ci: remove live-ref CI dispatcher'
          git push origin HEAD:main
