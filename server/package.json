{
  "name": "wafra-relay",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "setup": "node scripts/d1.mjs setup",
    "predeploy": "node scripts/d1.mjs check",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "migrate": "wrangler d1 execute wafra --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.20.0"
  }
}
