/**
 * Native binaries embed every product font through the expo-font config
 * plugin. Keeping this map empty also keeps Metro from bundling a second copy
 * of each face as a JavaScript asset.
 *
 * The web-specific sibling is selected by Metro for browser builds.
 */
export const WEB_FONTS = {};
